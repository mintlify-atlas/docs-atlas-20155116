---
title: Activate a user
description: Re-enable a deactivated user account so the user can authenticate again.
---

Sets `is_active` to `true` for the specified user, allowing them to authenticate. Use this endpoint to re-enable an account that was previously deactivated.

<Warning>
  This endpoint requires **admin** privileges. Requests from non-admin users return `403 Forbidden`.
</Warning>

## Authentication

Requires a valid Bearer token from an admin account in the `Authorization` header.

## Path parameters

<ParamField path="user_id" type="number" required>
  The unique integer ID of the user to activate.
</ParamField>

## Response fields

<ResponseField name="success" type="boolean" required>
  `true` when the activation succeeds.
</ResponseField>

<ResponseField name="message" type="string" required>
  Confirmation message.
</ResponseField>

<ResponseField name="data" type="object" required>
  The updated user object with `is_active: true`.
  <Expandable title="properties" defaultOpen={true}>
    <ResponseField name="id" type="number" required>
      Unique identifier for the user.
    </ResponseField>
    <ResponseField name="username" type="string" required>
      The user's unique username.
    </ResponseField>
    <ResponseField name="email" type="string" required>
      The user's email address.
    </ResponseField>
    <ResponseField name="first_name" type="string">
      User's first name. `null` if not set.
    </ResponseField>
    <ResponseField name="last_name" type="string">
      User's last name. `null` if not set.
    </ResponseField>
    <ResponseField name="full_name" type="string" required>
      Computed display name.
    </ResponseField>
    <ResponseField name="is_active" type="boolean" required>
      `true` after this operation completes.
    </ResponseField>
    <ResponseField name="role" type="object" required>
      The user's assigned role.
      <Expandable title="role properties">
        <ResponseField name="id" type="number" required>
          Role ID.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Role name: `admin` or `user`.
        </ResponseField>
        <ResponseField name="description" type="string">
          Role description.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="created_at" type="string" required>
      ISO 8601 timestamp of account creation.
    </ResponseField>
    <ResponseField name="updated_at" type="string" required>
      ISO 8601 timestamp of the most recent update.
    </ResponseField>
  </Expandable>
</ResponseField>

## Errors

| Status | Description |
| ------ | ----------- |
| `401`  | Missing or invalid Bearer token. |
| `403`  | Authenticated user is not an admin. |
| `404`  | No user exists with the given `user_id`. |

## Examples

<CodeGroup>

```bash cURL
curl --request POST \
  --url https://api.taskforge.example.com/api/users/2/activate \
  --header 'Authorization: Bearer <admin-token>'
```

```python Python
import requests

response = requests.post(
    "https://api.taskforge.example.com/api/users/2/activate",
    headers={"Authorization": "Bearer <admin-token>"},
)
print(response.json())
```

</CodeGroup>

### Response

```json 200
{
  "success": true,
  "message": "Usuario activado con exito",
  "data": {
    "id": 2,
    "username": "jdoe",
    "email": "jdoe@example.com",
    "first_name": "John",
    "last_name": "Doe",
    "full_name": "John Doe",
    "is_active": true,
    "role": {
      "id": 2,
      "name": "user",
      "description": "Standard user"
    },
    "created_at": "2024-02-01T08:00:00",
    "updated_at": "2024-03-15T17:00:00"
  }
}
```

```json 403
{
  "success": false,
  "error": "Prohibido"
}
```

```json 404
{
  "success": false,
  "error": "Usuario no encontrado"
}
```
