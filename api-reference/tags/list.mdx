---
title: List tags
description: Retrieve a paginated list of all tags.
---

Returns a paginated list of all tags in the system. Any authenticated user can call this endpoint.

## Authentication

Requires a valid Bearer token in the `Authorization` header.

## Query parameters

<ParamField query="page" type="number" default="1">
  Page number to retrieve.
</ParamField>

<ParamField query="per_page" type="number" default="50">
  Number of tags to return per page.
</ParamField>

<ParamField query="sort_by" type="string" default="name">
  Field to sort by. Accepted values: `id`, `name`, `created_at`, `updated_at`.
</ParamField>

<ParamField query="sort_order" type="string" default="asc">
  Sort direction. Accepted values: `asc`, `desc`.
</ParamField>

## Response fields

<ResponseField name="success" type="boolean" required>
  `true` when the request succeeds.
</ResponseField>

<ResponseField name="data" type="object" required>
  <Expandable title="properties" defaultOpen={true}>
    <ResponseField name="tags" type="object[]" required>
      Array of tag objects.
      <Expandable title="tag properties">
        <ResponseField name="id" type="number" required>
          Unique identifier for the tag.
        </ResponseField>
        <ResponseField name="name" type="string" required>
          Tag name. Unique across all tags.
        </ResponseField>
        <ResponseField name="color" type="string" required>
          Hex color code (e.g. `#FF5733`).
        </ResponseField>
        <ResponseField name="description" type="string">
          Optional description of the tag. `null` if not set.
        </ResponseField>
        <ResponseField name="task_count" type="number" required>
          Number of tasks currently assigned this tag.
        </ResponseField>
        <ResponseField name="created_at" type="string" required>
          ISO 8601 timestamp of when the tag was created.
        </ResponseField>
        <ResponseField name="updated_at" type="string" required>
          ISO 8601 timestamp of when the tag was last updated.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="pagination" type="object" required>
      Pagination metadata.
      <Expandable title="pagination properties">
        <ResponseField name="page" type="number" required>
          Current page number.
        </ResponseField>
        <ResponseField name="per_page" type="number" required>
          Number of items per page.
        </ResponseField>
        <ResponseField name="total" type="number" required>
          Total number of tags across all pages.
        </ResponseField>
        <ResponseField name="pages" type="number" required>
          Total number of pages.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Examples

<CodeGroup>

```bash cURL
curl --request GET \
  --url 'https://api.taskforge.example.com/api/tags?page=1&per_page=50&sort_by=name&sort_order=asc' \
  --header 'Authorization: Bearer <token>'
```

```python Python
import requests

response = requests.get(
    "https://api.taskforge.example.com/api/tags",
    headers={"Authorization": "Bearer <token>"},
    params={
        "page": 1,
        "per_page": 50,
        "sort_by": "name",
        "sort_order": "asc",
    },
)
print(response.json())
```

</CodeGroup>

### Response

```json 200
{
  "success": true,
  "data": {
    "tags": [
      {
        "id": 1,
        "name": "Backend",
        "color": "#3B82F6",
        "description": "Backend engineering tasks",
        "task_count": 14,
        "created_at": "2024-01-15T09:00:00",
        "updated_at": "2024-03-10T14:22:00"
      },
      {
        "id": 2,
        "name": "Frontend",
        "color": "#10B981",
        "description": null,
        "task_count": 7,
        "created_at": "2024-01-15T09:05:00",
        "updated_at": "2024-01-15T09:05:00"
      }
    ],
    "pagination": {
      "page": 1,
      "per_page": 50,
      "total": 2,
      "pages": 1
    }
  }
}
```
